extends base.pug

block content 
  if errorMessage
    span=errorMessage
  form(method="POST" action="/videos/upload")
    input(type="text" name="title" placeholder="Title" maxlength=80 required)
    input(type="text" name="description" placeholder="Description" minlength=20 required)
    input(type="text" name="hashtags" placeholder="Hashtags, seprated by comma" required)
    input(type="submit" value="Upload Video")